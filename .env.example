# Copy to .env.local for local dev, or set in your shell / CI / Cloudflare build env.
LUMA_API_KEY=your_luma_api_key_here

# Sponsor inquiry emails are sent via Cloudflare Email (send_email binding in wrangler.jsonc).
# Enable sending for your domain before deploy:
#   npx wrangler email sending enable cursorrwanda.com
